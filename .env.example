# Copy to `.env` and adjust. All values are optional — defaults keep the app in mock mode.

# Base URL of the future backend REST API.
VITE_API_BASE_URL=/api

# When "false", services call the Axios API client instead of the mock layer.
VITE_USE_MOCK_API=true
